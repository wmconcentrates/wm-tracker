{
  "name": "leaflink-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for LeafLink API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo 'No build needed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
